<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.elearningplus">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".AssignmentViewScreen"
            android:label="Assignment"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ChapterViewScreenActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".NotificationScreenActivity"
            android:label="Notification" />
        <activity
            android:name=".LoginScreenActivity"
            android:theme="@style/AppThemes"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".HomeScreenActivity"
            android:label="Home" />
        <activity android:name=".BottomTab" />
        <activity
            android:name=".ProfileScreenActivity"
            android:label="Profile" />
        <activity android:name=".CourseScreenActivity" />
        <activity
            android:name=".ChooseLoginSignup"
            android:theme="@style/AppThemes">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SignupScreenActivity"
            android:theme="@style/AppThemes"
            android:windowSoftInputMode="adjustResize" >

        </activity>
        <activity android:name=".LogUp_ChooseLesson_Activity">

        </activity>
    </application>

</manifest>